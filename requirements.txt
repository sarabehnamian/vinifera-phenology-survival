# Environment used to produce the results reported in
# "Fixed Pre-Season Weather Windows Eliminate Endogeneity Bias in
#  Interval-Censored Survival Models of Grapevine Phenology"
#
# Python 3.11.9 (Windows)
#
# Install with:
#     pip install -r requirements.txt
#
# Versions are pinned exactly. To run the pipeline on newer releases,
# relax "==" to ">=", but note that the published figures and tables were
# produced with the versions below.

# --- Core: required by the vinifera_phenology package ---
pandas==2.2.0
numpy==1.26.4
openpyxl==3.1.2          # pandas engine for .xlsx read/write
requests==2.32.5         # NASA POWER API queries (02_..., vinifera_phenology/weather.py)

# --- Survival models ---
# scripts 05, 07, 09, 10, 12, 13; supplementary_analyses/A1, A3, A4, S2;
# and the sensitivity refits: base_temperature_sensitivity,
# homogeneous_subset, bbch_intensity_endpoint, recovery_check,
# water_balance, window_length_sensitivity
lifelines==0.30.0

# --- Statistics and diagnostics ---
# scipy:        supplementary_analyses/A3, A4, A5, S2, S3, S4
# statsmodels:  supplementary_analyses/A5 -- cluster-robust standard errors
#               reported in "Hierarchical Structure and Dependence" and used
#               for the approximate clustering correction in that subsection.
#               NOTE: this import sits inside a try/except that returns None
#               on failure, so a missing statsmodels yields no error, only a
#               missing result. Verify statsmodels is installed before
#               trusting the SE inflation factors (0.72 and 2.08).
# scikit-learn: supplementary_analyses/A2 -- least-squares fits in the
#               endogeneity simulation
scipy==1.12.0
statsmodels==0.14.1
scikit-learn==1.7.2

# --- Figures ---
# scripts 04, 08, 09, 11-15 and most supplementary_analyses/ scripts
matplotlib==3.8.2
seaborn==0.13.2

# --- Mapping (Figure 1) ---
# supplementary_analyses/site_map.py -- Albers equal-area conic projection,
# geodesic scale bar, meridian-aligned north arrow, Natural Earth I raster
cartopy==0.22.0
pillow==12.3.0